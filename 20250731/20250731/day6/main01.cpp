#include <iostream>

int main()
{
	/*스무고개 (1~100사이 랜덤한 수를 사용자가 20번내에 맞추는 게임
	 srand((unsigned)timeNULL))
	 int collect num = rand()%101 
	 int num=0;
	 printf("랜덤수를 맞춰볼래?");
	 scanf_s("%d",&num);

	 for(int i=0; i<20; i++) 
	    if (num> collect num)
			printf("커");
		else if (num < collect num);
			printf("작아");
		else	
			printf("좋았어!")
			break;
		
	printf("너는 졌어");
	printf("정답은 %d",collect num);
			}
			*/